# =============================================================================
# TUTORIAL: Environment Variables Configuration
# =============================================================================
#
# WHY ENVIRONMENT VARIABLES?
# --------------------------
# Following the 12-Factor App methodology, configuration should be stored in
# the environment, not in code. This allows:
#   - Different configs for dev/staging/prod without code changes
#   - Secrets stay out of version control
#   - Easy container orchestration (Docker, K8s inject env vars)
#
# HOW TO USE:
# -----------
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values
# 3. NEVER commit .env to git (it's in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# Google API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key-here

# -----------------------------------------------------------------------------
# Gemini Model Configuration
# -----------------------------------------------------------------------------
# Model for text generation
# Options (Gemini 2.5 - ordered by cost, cheapest first):
#   - gemini-2.5-flash-preview-05-20    (cheapest, recommended)
#   - gemini-2.5-pro-preview-05-06      (more capable, higher cost)
#
# Note: We use gemini-2.5-flash as it's the cheapest Gemini 2.5 model
GEMINI_MODEL=gemini-2.5-flash-preview-05-20

# Generation temperature (0.0-1.0)
# Lower = more focused/deterministic, Higher = more creative/random
GEMINI_TEMPERATURE=0.2

# Maximum output tokens for generation
GEMINI_MAX_TOKENS=2048

# -----------------------------------------------------------------------------
# Embedding Model Configuration
# -----------------------------------------------------------------------------
# Model for text embeddings (converting text to vectors)
# text-embedding-004 is Google's latest embedding model
EMBEDDING_MODEL=models/text-embedding-004

# -----------------------------------------------------------------------------
# ChromaDB Configuration
# -----------------------------------------------------------------------------
# Path to ChromaDB persistent storage
CHROMA_PERSIST_DIR=./data/chromadb

# Collection name for Jira tickets
CHROMA_COLLECTION=jira_issues

# -----------------------------------------------------------------------------
# RAG Configuration
# -----------------------------------------------------------------------------
# Number of similar documents to retrieve
RAG_TOP_K=6

# Chunk size for text splitting (characters)
CHUNK_SIZE=1200

# Overlap between chunks (characters)
CHUNK_OVERLAP=200

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for FastAPI server
PORT=8000

# Environment (development/staging/production)
ENV=development

# Log level (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Allowed origins for cross-origin requests (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:80

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# Backend API URL (used during frontend build)
REACT_APP_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Jira Cloud Integration (OPTIONAL - for auto-creating tickets)
# -----------------------------------------------------------------------------
# The agent can automatically create tickets in your Jira board!
# Set these to enable the create_jira_ticket tool.

# Your Jira Cloud instance URL
# Example: https://your-company.atlassian.net
JIRA_BASE_URL=https://your-domain.atlassian.net

# Your Atlassian account email
JIRA_EMAIL=your-email@company.com

# Jira API token (NOT your password)
# Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your-api-token-here

# Default project key for creating issues
JIRA_PROJECT_KEY=PROJ

# (Optional) Default assignee account ID
# Find this in Jira user profile URL or API response
# Example: 712020:690de3f9-4a74-4bd2-b5bf-495f7e62ea96
JIRA_ASSIGNEE_ACCOUNT_ID=

# (Optional) Custom field ID for start date
# This varies by Jira project - check your project's fields
# Example: customfield_10045
JIRA_START_DATE_FIELD=

# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit .env files with real credentials
# 2. Use different API keys for dev/staging/prod
# 3. Rotate API keys periodically
# 4. In production, use secrets management (AWS Secrets Manager, HashiCorp Vault)
#
# =============================================================================
